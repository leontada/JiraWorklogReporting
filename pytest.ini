[pytest]
# Measure coverage from the src/ package path to avoid early-import/aliasing issues
addopts = -q --cov=src/jira_worklog_extractor --cov-report=term-missing --cov-fail-under=95
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
