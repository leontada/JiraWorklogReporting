# Environment variable fallback for mJiraWorkLogExtractor
# Copy this file to ".env" (do NOT commit) and fill in your values, or export in your shell/CI.

# Required
JIRA_BASE_URL=https://yourcompany.atlassian.net
JIRA_EMAIL=your.name@yourcompany.com
JIRA_API_TOKEN=your_api_token

# Optional date overrides (YYYY-MM-DD). If omitted, start defaults to first day of current month, end to today.
#JIRA_START_DATE=2025-10-01
#JIRA_END_DATE=2025-10-31
